const request = require('supertest')
const server = require('./server')

describe('Products', () => {
    it('GET /api/products --> get all the products', () => { });
    
    it('GET /api/products/id --> specii')
})
